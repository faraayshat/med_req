rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated users can access their own folder under /medical-files/
    match /medical-files/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
